apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vprofile-credentail
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/sinchan62/vprofile-Argocd.git
    targetRevision: test-env
    path: temp-res
  destination: 
    server: https://kubernetes.default.svc
    namespace: myapp

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true